---
description: resume the last interrupted team run
---

Resume the last interrupted team run for the current project.

Rules:

- Use the `TeamResume` tool exactly once.
- If there is nothing resumable, state that clearly.

After the tool call:

- Summarize how many tasks were resumed.

$ARGUMENTS
