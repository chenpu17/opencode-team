---
description: inspect current team status, phase, round, resume, and memory markers
---

Inspect the current team status for the current project.

Rules:

- Use the `TeamStatus` tool exactly once.

After the tool call:

- Summarize execution state, run phase, run round, whether resume is available, and key member/task highlights.

$ARGUMENTS
